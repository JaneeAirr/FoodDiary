# 📊 Сводка по импорту продуктов

## ✅ Текущее состояние базы данных

### Импортировано:
- **88 продуктов** из встроенной базы (bulk import)
- Категории: белки, углеводы, овощи, фрукты, молочные, орехи, бобовые

### USDA API:
- ✅ API ключ настроен и работает
- ✅ Тест поиска успешен (найдено 401 результат для "chicken")
- ✅ Импортер готов к использованию

---

## 🚀 Как расширить базу

### Вариант 1: Быстрый импорт (уже сделано)
```bash
cd backend
python manage.py import_foods_bulk --category all
```
**Результат:** 88 продуктов импортировано

### Вариант 2: Импорт из USDA (рекомендуется)

**Импорт популярных продуктов:**
```bash
python manage.py import_usda_foods --popular --limit 100 --skip-existing
```
Это добавит ~100-200 продуктов из USDA базы.

**Импорт по поиску:**
```bash
python manage.py import_usda_foods --search "chicken" "beef" "fish" --limit 20
```

**Импорт по категориям:**
```bash
# Белки
python manage.py import_usda_foods --search "chicken breast" "salmon" "eggs" "beef" "turkey" --limit 30

# Углеводы
python manage.py import_usda_foods --search "rice" "pasta" "bread" "potato" "oats" --limit 30

# Овощи
python manage.py import_usda_foods --search "broccoli" "spinach" "carrot" "tomato" "cucumber" --limit 30

# Фрукты
python manage.py import_usda_foods --search "apple" "banana" "orange" "berry" "grape" --limit 30
```

---

## 📈 Потенциал расширения

### Текущая база:
- **88 продуктов** (встроенная база)

### После импорта из USDA:
- **200-500 продуктов** (популярные продукты)
- **1000+ продуктов** (расширенный импорт)
- **10,000+ продуктов** (полный импорт, требует времени)

### USDA база данных:
- **300,000+ продуктов** доступно через API
- Foundation Foods (основные продукты)
- SR Legacy (старая база)
- Branded Foods (брендированные продукты)

---

## ⚙️ Настройки

### API ключ USDA:
✅ Добавлен в `backend/.env`:
```
USDA_API_KEY=Ons5jduMe5IGXLpJ1bdyiei3pjQYFzoklaTaPgYg
```

### Rate Limits:
- USDA API: 1000 запросов/час
- Импортер автоматически добавляет задержки
- Рекомендуется импортировать постепенно

---

## 🎯 Рекомендации

1. **Начните с популярных продуктов:**
   ```bash
   python manage.py import_usda_foods --popular --limit 100
   ```

2. **Добавляйте по категориям:**
   Импортируйте по 20-30 продуктов за раз для разных категорий

3. **Используйте --skip-existing:**
   Чтобы не создавать дубликаты при повторном импорте

4. **Мониторьте базу:**
   ```bash
   python manage.py shell
   >>> from api.models import Food
   >>> Food.objects.count()
   >>> Food.objects.filter(data_source='usda').count()
   ```

---

## 📝 Команды для быстрого старта

```bash
# 1. Импорт популярных продуктов из USDA (100 продуктов)
python manage.py import_usda_foods --popular --limit 100 --skip-existing

# 2. Импорт конкретных категорий
python manage.py import_usda_foods --search "chicken" "beef" "pork" --limit 30 --skip-existing

# 3. Проверка количества продуктов
python manage.py shell -c "from api.models import Food; print('Total:', Food.objects.count())"
```

---

Готово! Система импорта настроена и готова к использованию! 🎉




