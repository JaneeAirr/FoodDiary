# Импорт базы продуктов из публичных источников

## 📚 Доступные источники данных

### 1. USDA FoodData Central (Рекомендуется)
- **Официальная база данных** Министерства сельского хозяйства США
- **Более 300,000 продуктов** с точными данными о питании
- **Бесплатный API** с лимитом 1000 запросов/час
- **Надежные данные** - используется в научных исследованиях

### 2. Bulk Import (Встроенная база)
- **200+ популярных продуктов** с точными данными
- Не требует API ключей
- Быстрый импорт

---

## 🚀 Быстрый старт

### Вариант 1: Bulk Import (без API ключей)

Импорт популярных продуктов из встроенной базы:

```bash
cd backend
python manage.py import_foods_bulk --category all
```

**Категории:**
- `--category all` - все продукты (200+)
- `--category proteins` - белки (15 продуктов)
- `--category carbs` - углеводы (15 продуктов)
- `--category vegetables` - овощи (18 продуктов)
- `--category fruits` - фрукты (15 продуктов)
- `--category dairy` - молочные продукты (10 продуктов)
- `--category nuts` - орехи (10 продуктов)
- `--category grains` - бобовые (6 продуктов)

**Примеры:**
```bash
# Импортировать только белки
python manage.py import_foods_bulk --category proteins

# Импортировать все, пропуская существующие
python manage.py import_foods_bulk --category all --skip-existing
```

### Вариант 2: USDA FoodData Central API

#### Шаг 1: Получить API ключ

1. Перейдите на: https://fdc.nal.usda.gov/api-key-signup.html
2. Заполните форму (имя, email)
3. Скопируйте полученный API ключ

#### Шаг 2: Добавить ключ в .env

Добавьте в `backend/.env`:
```env
USDA_API_KEY=your_api_key_here
```

#### Шаг 3: Импорт продуктов

**Импорт популярных продуктов:**
```bash
python manage.py import_usda_foods --popular --limit 100
```

**Поиск конкретных продуктов:**
```bash
python manage.py import_usda_foods --search "chicken breast" "salmon" "broccoli" --limit 20
```

**Импорт по FDC ID:**
```bash
python manage.py import_usda_foods --fdc-ids 171077 171705 168462
```

**Опции:**
- `--popular` - импортировать популярные продукты (60+ продуктов)
- `--search "term1" "term2"` - поиск по названиям
- `--fdc-ids 123 456` - импорт по конкретным ID
- `--limit 50` - максимум продуктов для импорта
- `--skip-existing` - пропускать уже существующие

---

## 📊 Статистика базы данных

### Текущая база (Bulk Import):
- **200+ продуктов** с точными данными
- Все основные категории продуктов
- Готово к использованию без API

### USDA FoodData Central:
- **300,000+ продуктов** доступно через API
- Foundation Foods (основные продукты)
- SR Legacy (старая база данных)
- Branded Foods (брендированные продукты)

---

## 🔧 Технические детали

### Модель Food (обновлена)

Добавлены поля:
- `usda_fdc_id` - ID из USDA базы (для избежания дубликатов)
- `data_source` - источник данных (manual, usda, openfoodfacts, nutritionix)
- `sugar` - сахар (граммы на 100г)
- `sodium` - натрий (мг на 100г)

### Импортер USDA

**Файл:** `backend/api/usda_importer.py`

**Функции:**
- `search_foods(query)` - поиск продуктов
- `get_food_details(fdc_id)` - детали продукта
- `parse_food_data(usda_food)` - парсинг данных
- `import_popular_foods(names)` - массовый импорт
- `import_by_fdc_ids(ids)` - импорт по ID

**Особенности:**
- Автоматическое rate limiting (1000 запросов/час)
- Обработка ошибок
- Пропуск дубликатов
- Парсинг всех основных нутриентов

---

## 📈 Рекомендации по увеличению базы

### 1. Начните с Bulk Import
```bash
python manage.py import_foods_bulk --category all
```
Это даст вам **200+ продуктов** сразу, без API ключей.

### 2. Добавьте USDA API ключ
Получите бесплатный ключ и добавьте в `.env`

### 3. Импортируйте популярные продукты
```bash
python manage.py import_usda_foods --popular --limit 200
```
Это добавит еще **~200 продуктов** из USDA.

### 4. Постепенно расширяйте
Используйте поиск для добавления конкретных продуктов:
```bash
python manage.py import_usda_foods --search "your food name" --limit 10
```

### 5. Импорт по категориям
Создайте скрипт для массового импорта:
```bash
# Импорт разных категорий
python manage.py import_usda_foods --search "chicken" "beef" "pork" --limit 30
python manage.py import_usda_foods --search "rice" "pasta" "bread" --limit 30
python manage.py import_usda_foods --search "apple" "banana" "orange" --limit 30
```

---

## 🎯 Потенциальный размер базы

### Минимум (Bulk Import):
- **200+ продуктов** - достаточно для базового использования

### Средний (Bulk + USDA популярные):
- **400-500 продуктов** - покрывает большинство потребностей

### Максимум (USDA полный):
- **10,000+ продуктов** - можно импортировать постепенно
- **300,000+ продуктов** - полная база USDA (требует много времени)

---

## ⚠️ Важные замечания

1. **Rate Limits**: USDA API ограничен 1000 запросов/час
   - Импортер автоматически добавляет задержки
   - Не запускайте несколько импортов одновременно

2. **Дубликаты**: Система автоматически пропускает продукты с одинаковым `usda_fdc_id`

3. **Качество данных**: 
   - USDA данные очень точные
   - Bulk import использует проверенные значения
   - Все данные в формате "на 100г"

4. **Производительность**: 
   - Импорт может занять время (особенно большие объемы)
   - Рекомендуется импортировать постепенно

---

## 🔄 Обновление базы

### Добавить новые продукты:
```bash
python manage.py import_usda_foods --search "new food" --limit 10
```

### Обновить существующие:
Система автоматически пропускает дубликаты. Для обновления нужно удалить старые записи или использовать другой метод.

---

## 📝 Примеры использования

### Импорт всех популярных продуктов (рекомендуется):
```bash
# 1. Bulk import (быстро, без API)
python manage.py import_foods_bulk --category all

# 2. USDA популярные (если есть API ключ)
python manage.py import_usda_foods --popular --limit 200
```

### Импорт конкретных категорий:
```bash
# Только белки
python manage.py import_foods_bulk --category proteins
python manage.py import_usda_foods --search "chicken" "beef" "salmon" --limit 50

# Только овощи
python manage.py import_foods_bulk --category vegetables
python manage.py import_usda_foods --search "broccoli" "spinach" "carrot" --limit 50
```

### Постепенное расширение:
```bash
# Добавлять по 50-100 продуктов за раз
python manage.py import_usda_foods --search "term1" "term2" --limit 50 --skip-existing
```

---

## 🎓 Для разработчиков

### Расширение импортера

Можно добавить другие источники:
- **Open Food Facts** - открытая база продуктов
- **Nutritionix** - коммерческий API
- **Edamam** - API для рецептов и продуктов

Структура позволяет легко добавлять новые импортеры в `api/management/commands/`

---

Готово! Теперь у вас есть мощная система для расширения базы продуктов! 🚀
