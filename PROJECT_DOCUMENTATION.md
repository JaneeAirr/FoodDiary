# 📚 Документация проекта Food Diary

Полное руководство по структуре, функциям и архитектуре проекта для объяснения и понимания системы.

---

## 🎯 Общая концепция проекта

**Food Diary** - это веб-приложение для отслеживания питания с AI-ассистентом. Пользователи могут:
- Входить в систему (обычная регистрация или через Google)
- Записывать приемы пищи
- Отслеживать калории и БЖУ (белки, жиры, углеводы)
- Получать персональные рекомендации от AI
- Видеть статистику и графики
- Получать напоминания о приемах пищи

---

## 📁 Структура проекта

```
ExamWebProject/
├── backend/                    # Django сервер (API)
│   ├── api/                   # Основная логика приложения
│   ├── users/                 # Аутентификация пользователей
│   ├── ai/                    # AI сервисы (OpenAI, Gemini, Claude)
│   ├── food_diary/            # Настройки Django проекта
│   └── manage.py              # Управление Django
│
└── frontend/                   # React приложение (UI)
    └── src/
        ├── components/         # Переиспользуемые компоненты
        ├── pages/              # Страницы приложения
        ├── services/           # API вызовы и сервисы
        └── store/              # Управление состоянием
```

---

## 🔧 Backend (Django) - Детальное описание

### 1. `backend/api/` - Основная логика приложения

#### **models.py** - Модели базы данных

**Food** - Продукты питания
- Хранит информацию о продуктах (название, бренд, калории, БЖУ на 100г)
- Используется как справочник продуктов

**Meal** - Приемы пищи
- Связывает пользователя с продуктом
- Хранит: дату, тип приема пищи (завтрак/обед/ужин/перекус), количество в граммах
- **Важно**: Калории и БЖУ вычисляются автоматически через свойства (`total_calories`, `total_protein`, etc.)

**NutritionGoal** - Цели по питанию
- Хранит дневные цели пользователя (калории, белки, углеводы, жиры)
- Автоматически рассчитывается при заполнении профиля

**WeightEntry** - Записи веса
- История веса пользователя по датам
- Для графиков прогресса

**Notification** - Уведомления
- Системные уведомления для пользователя
- Типы: reminder, warning, info, achievement

**MealReminderSettings** - Настройки напоминаний
- Время для каждого приема пищи
- Дни недели, когда активны напоминания
- Настройки браузерных уведомлений

#### **views.py** - API endpoints (логика обработки запросов)

**FoodViewSet** - Поиск продуктов
- `GET /api/foods/` - список продуктов с поиском

**MealViewSet** - Управление приемами пищи
- `GET /api/meals/` - список приемов пищи (можно фильтровать по дате)
- `POST /api/meals/` - добавить прием пищи
- `PUT/PATCH /api/meals/{id}/` - изменить прием пищи
- `DELETE /api/meals/{id}/` - удалить прием пищи

**daily_summary** - Дневная сводка
- `GET /api/daily-summary/?date=YYYY-MM-DD`
- Возвращает: общие калории/БЖУ за день, список приемов пищи, цели пользователя
- **Важно**: Суммирует все приемы пищи за указанную дату

**statistics** - Статистика за период
- `GET /api/statistics/?start_date=YYYY-MM-DD&end_date=YYYY-MM-DD`
- Возвращает: данные по дням (калории, БЖУ), данные по весу
- Группирует данные по датам для графиков

**calculate_nutrition** - Расчет БЖУ
- `POST /api/calculate-nutrition/`
- Принимает: вес, рост, возраст, пол, активность, цель
- Возвращает: BMR, TDEE, дневные калории, макронутриенты
- Использует формулу Харриса-Бенедикта

#### **utils.py** - Вспомогательные функции

- `calculate_bmr()` - Базальный метаболизм (сколько калорий сжигает тело в покое)
- `calculate_tdee()` - Общий расход энергии (BMR × активность)
- `calculate_daily_calories()` - Целевые калории (TDEE ± дефицит/профицит)
- `calculate_macros()` - Расчет БЖУ на основе калорий и цели

#### **serializers.py** - Преобразование данных

Преобразуют модели Django в JSON и обратно:
- `FoodSerializer` - для продуктов
- `MealSerializer` - для приемов пищи (включает вычисленные значения)
- `NutritionGoalSerializer` - для целей
- И т.д.

### 2. `backend/users/` - Аутентификация

#### **models.py** - Модель пользователя

**User** (расширенная модель Django User)
- Стандартные поля: username, email, password
- Дополнительные: рост, вес, дата рождения, пол, цель (похудение/набор/поддержание), уровень активности
- Настройки: тема, диетические предпочтения

#### **views.py** - Endpoints аутентификации

- `POST /api/auth/register/` - Регистрация нового пользователя
- `POST /api/auth/login/` - Вход (возвращает JWT токены)
- `GET /api/auth/profile/` - Получить профиль текущего пользователя
- `PUT /api/auth/profile/update/` - Обновить профиль
  - **Важно**: При обновлении профиля автоматически пересчитываются цели по питанию

#### **google_auth.py** - Google OAuth

- `GET /api/auth/google/` - Получить URL для авторизации через Google
- `GET /api/auth/google/callback/` - Обработка ответа от Google
  - Обменивает код на токен
  - Получает данные пользователя из Google
  - Создает/находит пользователя в базе
  - Возвращает JWT токены

### 3. `backend/ai/` - AI сервисы

#### **services.py** - Основная логика AI

**Мультипровайдерная система** (3 провайдера с автоматическим fallback):
1. **Anthropic Claude** (приоритет 1)
2. **Google Gemini** (приоритет 2)
3. **OpenAI GPT-4o-mini** (приоритет 3)
4. **Rule-based fallback** (если все API недоступны)

**Функции:**

`chat_with_ai(user, message, conversation_history)`
- Обычный чат с AI
- Передает контекст пользователя (цели, история питания)
- Использует fallback если API недоступны

`generate_meal_plan(user, days, preferences)`
- Генерирует план питания на N дней
- Учитывает цели пользователя и доступные продукты
- Возвращает структурированный JSON с блюдами

`analyze_habits(user, days=7)`
- Анализирует пищевые привычки за период
- Ищет паттерны (поздние приемы пищи, превышение калорий, нехватка белка)
- Возвращает рекомендации

#### **views.py** - AI endpoints

- `POST /api/ai/chat/` - Отправить сообщение AI
- `POST /api/ai/generate-meal-plan/` - Сгенерировать план питания
- `GET /api/ai/analyze-habits/` - Анализ привычек

---

## 🎨 Frontend (React) - Детальное описание

### 1. `src/App.js` - Главный компонент

**Что делает:**
- Настраивает тему (темная/светлая)
- Управляет роутингом (React Router)
- Запускает сервис напоминаний о приемах пищи
- Оборачивает приложение в AuthProvider

**Важные части:**
- `AppContent` - основной компонент с роутингом
- `PrivateRoute` - защищает страницы (требует авторизации)
- `ThemeProvider` - управление темой

### 2. `src/store/AuthContext.js` - Управление авторизацией

**Состояние:**
- `user` - данные текущего пользователя
- `token` - JWT токен
- `loading` - загрузка данных

**Функции:**
- `login(username, password)` - вход
- `register(userData)` - регистрация
- `logout()` - выход
- `fetchProfile()` - загрузка профиля

**Как работает:**
- При входе сохраняет токены в `localStorage`
- При каждом запросе добавляет токен в заголовки
- При ошибке авторизации автоматически выходит

### 3. `src/services/api.js` - API клиент

**Что делает:**
- Настраивает Axios с базовым URL
- Добавляет JWT токен в заголовки каждого запроса
- Обрабатывает ошибки авторизации

### 4. `src/services/reminderService.js` - Напоминания о приемах пищи

**Как работает:**
- Проверяет время каждую минуту
- Сравнивает текущее время с настройками пользователя
- Показывает браузерные уведомления в нужное время
- Работает только в активные дни недели

### 5. Страницы (`src/pages/`)

#### **Dashboard.js** - Главная страница
- Показывает дневную сводку (калории, БЖУ)
- Прогресс-бары для каждого макронутриента
- Список приемов пищи за день
- Баннер с призывом заполнить профиль (если не заполнен)

**Ключевые данные:**
- `summary.totals` - общие калории/БЖУ за день
- `summary.goals` - цели пользователя
- `summary.meals` - список приемов пищи

#### **Diary.js** - Дневник питания
- Календарь для выбора даты
- Список приемов пищи за выбранную дату
- Форма добавления/редактирования приема пищи
- Поиск продуктов

#### **Foods.js** - База продуктов
- Поиск продуктов
- Список всех продуктов с информацией о БЖУ

#### **Statistics.js** - Статистика
- Графики калорий по дням
- Графики БЖУ
- График веса
- Фильтр по датам

#### **AI.js** - AI ассистент
- **Вкладка "Chat"**: Чат с AI
- **Вкладка "Meal Plan"**: Генерация плана питания
- **Вкладка "Habits Analysis"**: Анализ привычек

#### **Profile.js** - Профиль и настройки
- **Вкладка "Profile"**: Основная информация (рост, вес, пол, дата рождения, цель, активность)
- **Вкладка "Goals"**: Ручная настройка целей по калориям и БЖУ
- **Вкладка "Calculations"**: Показывает расчеты (BMR, TDEE, дневные калории)
- **Вкладка "Reminders"**: Настройки напоминаний о приемах пищи
- **Вкладка "Account"**: Выход из аккаунта

#### **Weight.js** - Отслеживание веса
- Добавление записи веса
- График изменения веса
- История записей

#### **Notifications.js** - Уведомления
- Список всех уведомлений
- Отметка прочитанных
- Фильтр по типу

### 6. Компоненты (`src/components/`)

#### **Sidebar.js** - Боковая панель (десктоп)
- Навигация по страницам
- Показывает имя пользователя
- Кнопка выхода
- Скрыта на мобильных устройствах

#### **MobileNav.js** - Нижняя навигация (мобильные)
- Иконки основных страниц
- Показывается только на мобильных устройствах

#### **MealReminderSettings.js** - Настройки напоминаний
- Выбор времени для каждого приема пищи
- Выбор активных дней недели
- Включение/выключение уведомлений
- Запрос разрешения на браузерные уведомления

#### **PrivateRoute.js** - Защита маршрутов
- Проверяет авторизацию пользователя
- Перенаправляет на `/login` если не авторизован

---

## 🔄 Потоки данных

### Регистрация/Вход
1. Пользователь заполняет форму → `AuthContext.register/login()`
2. Отправка на `/api/auth/register/` или `/api/auth/login/`
3. Backend создает/проверяет пользователя → возвращает JWT токены
4. Frontend сохраняет токены в `localStorage`
5. Автоматическая загрузка профиля → `fetchProfile()`

### Добавление приема пищи
1. Пользователь выбирает продукт и количество → `Diary.js`
2. Отправка на `POST /api/meals/` с `food_id`, `quantity`, `date`, `meal_type`
3. Backend создает `Meal` объект
4. Автоматически вычисляются калории и БЖУ (через свойства модели)
5. Возвращается созданный объект
6. Frontend обновляет список приемов пищи

### Получение дневной сводки
1. `Dashboard.js` запрашивает `/api/daily-summary/?date=YYYY-MM-DD`
2. Backend:
   - Находит все приемы пищи за дату
   - Суммирует калории и БЖУ (вручную, т.к. это свойства, не поля БД)
   - Получает цели пользователя
3. Возвращает JSON с totals, goals, meals
4. Frontend отображает данные и прогресс-бары

### AI чат
1. Пользователь отправляет сообщение → `AI.js`
2. Отправка на `POST /api/ai/chat/` с сообщением и историей
3. Backend:
   - Собирает контекст пользователя (профиль, цели, история питания)
   - Вызывает `chat_with_ai()` из `ai/services.py`
   - Пробует провайдеры по порядку (Claude → Gemini → OpenAI → fallback)
4. Возвращает ответ AI
5. Frontend добавляет ответ в историю чата

---

## 🗄️ База данных

### Основные таблицы:

**users_user** - Пользователи
- id, username, email, password, first_name, last_name
- height, weight, date_of_birth, gender, goal, activity_level

**api_food** - Продукты
- id, name, brand, calories, protein, carbs, fat (на 100г)

**api_meal** - Приемы пищи
- id, user_id, food_id, date, meal_type, quantity
- created_at, updated_at

**api_nutritiongoal** - Цели по питанию
- id, user_id, daily_calories, daily_protein, daily_carbs, daily_fat

**api_weightentry** - Записи веса
- id, user_id, weight, date, notes

**api_notification** - Уведомления
- id, user_id, notification_type, title, message, is_read

**api_mealremindersettings** - Настройки напоминаний
- id, user_id, reminders_enabled, breakfast_time, lunch_time, dinner_time, snack_time, active_days

---

## 🔐 Аутентификация

### JWT токены
- **Access token** - для API запросов (живет 1 час)
- **Refresh token** - для обновления access token (живет 7 дней)
- Хранятся в `localStorage` на клиенте
- Отправляются в заголовке `Authorization: Bearer <token>`

### Google OAuth
1. Пользователь нажимает "Continue with Google"
2. Frontend запрашивает `/api/auth/google/` → получает URL
3. Перенаправление на Google
4. Google перенаправляет на `/api/auth/google/callback/?code=...`
5. Backend обменивает код на токен, получает данные пользователя
6. Создает/находит пользователя, возвращает JWT токены
7. Frontend сохраняет токены и перенаправляет на Dashboard

---

## 🤖 AI интеграция

### Провайдеры (в порядке приоритета):
1. **Anthropic Claude** - `claude-3-haiku-20240307`
2. **Google Gemini** - `models/gemini-2.5-flash`
3. **OpenAI** - `gpt-4o-mini`
4. **Rule-based fallback** - простые ответы без API

### Как работает fallback:
- Если API ключ не настроен → использует fallback
- Если API вернул ошибку → пробует следующий провайдер
- Если все провайдеры недоступны → использует fallback

### Fallback ответы:
- Базовые ответы на популярные вопросы
- Рекомендации по питанию на основе правил
- Не требует API ключей

---

## 📱 Адаптивность

### Десктоп:
- Sidebar слева (240px)
- Основной контент справа от Sidebar
- Полная навигация в Sidebar

### Мобильные:
- Sidebar скрыт
- MobileNav внизу экрана
- Адаптивные Grid'ы (xs, sm, md breakpoints)
- Уменьшенные шрифты и отступы

---

## 🎨 Дизайн

### Цветовая схема:
- **Primary**: Зеленый (#4CAF50) - здоровье/питание
- **Secondary**: Оранжевый (#FF9800) - акценты
- **Success**: Зеленый - для положительных индикаторов
- **Error**: Красный (#F44336) - для предупреждений

### Градиенты:
- Заголовки используют градиентный текст
- Кнопки с градиентным фоном
- Карточки с градиентными фонами

### Компоненты:
- Карточки с тенями и hover эффектами
- Плавные анимации (transition)
- Скругленные углы (12-20px)

---

## 🔧 Важные технические детали

### Почему калории/БЖУ вычисляются вручную в views?
- `total_calories`, `total_protein` и т.д. - это **свойства** модели (properties), не поля БД
- Django ORM не может использовать `Sum()` на свойствах
- Поэтому в `daily_summary` и `statistics` мы вручную итерируемся по объектам и суммируем

### Как работает автоматический расчет целей?
- При сохранении профиля (`update_profile`) проверяются все поля
- Если все заполнено → вызываются функции из `utils.py`
- Рассчитываются BMR, TDEE, дневные калории, БЖУ
- Сохраняются в `NutritionGoal`

### Как работают напоминания?
- `reminderService.js` запускается при входе пользователя
- Каждую минуту проверяет время
- Сравнивает с настройками из `MealReminderSettings`
- Показывает браузерное уведомление если время совпало
- Работает только в активные дни недели

---

## 📊 Ключевые формулы

### BMR (Базальный метаболизм) - Харрис-Бенедикт:
- **Мужчины**: BMR = 88.362 + (13.397 × вес) + (4.799 × рост) - (5.677 × возраст)
- **Женщины**: BMR = 447.593 + (9.247 × вес) + (3.098 × рост) - (4.330 × возраст)

### TDEE (Общий расход энергии):
- TDEE = BMR × коэффициент активности
- Коэффициенты: 1.2 (сидячий) → 1.9 (очень активный)

### Целевые калории:
- Похудение: TDEE - 500 ккал
- Набор веса: TDEE + 500 ккал
- Поддержание: TDEE

### Макронутриенты:
- **Белки**: 30% от калорий ÷ 4 = граммы
- **Углеводы**: 40% от калорий ÷ 4 = граммы
- **Жиры**: 30% от калорий ÷ 9 = граммы

---

## 🚀 Как объяснить проект

### Краткое описание (30 секунд):
"Это веб-приложение для отслеживания питания с AI-ассистентом. Пользователи записывают приемы пищи, система автоматически считает калории и БЖУ, показывает прогресс к целям, и AI дает персональные рекомендации."

### Технический стек (1 минута):
"Backend на Django REST Framework - REST API с JWT аутентификацией. Frontend на React с Material-UI. База данных SQLite. AI интеграция через 3 провайдера с автоматическим fallback. Google OAuth для быстрого входа."

### Ключевые фичи (2 минуты):
1. **Автоматический расчет БЖУ** - формула Харриса-Бенедикта на сервере
2. **AI ассистент** - мультипровайдерная система (Claude, Gemini, OpenAI) с fallback
3. **Умные напоминания** - браузерные уведомления в установленное время
4. **Визуализация данных** - графики и прогресс-бары
5. **Адаптивный дизайн** - работает на всех устройствах

### Архитектура (3 минуты):
"Разделение на backend (API) и frontend (SPA). Backend обрабатывает бизнес-логику, расчеты, работу с БД, AI запросы. Frontend отвечает за UI, взаимодействие с пользователем, визуализацию. Коммуникация через REST API с JWT токенами."

---

## 📝 Полезные команды

### Backend:
```bash
python manage.py makemigrations  # Создать миграции
python manage.py migrate         # Применить миграции
python manage.py createsuperuser # Создать админа
python manage.py runserver       # Запустить сервер
```

### Frontend:
```bash
npm start      # Запустить dev сервер
npm build      # Собрать для production
npm test       # Запустить тесты
```

---

## 🐛 Отладка

### Проблемы с API:
- Проверить что backend запущен на порту 8000
- Проверить CORS настройки в `settings.py`
- Проверить токены в `localStorage` (DevTools → Application)

### Проблемы с данными:
- Проверить миграции: `python manage.py showmigrations`
- Проверить данные в админке: `http://localhost:8000/admin/`

### Проблемы с AI:
- Проверить API ключи в `.env`
- Проверить логи в консоли браузера и терминале Django
- AI автоматически использует fallback если API недоступны

---

## 📌 Важные файлы для понимания

1. **backend/api/models.py** - структура данных
2. **backend/api/views.py** - логика API endpoints
3. **backend/api/utils.py** - формулы расчета
4. **backend/ai/services.py** - AI логика
5. **frontend/src/App.js** - структура приложения
6. **frontend/src/store/AuthContext.js** - управление авторизацией
7. **frontend/src/pages/Dashboard.js** - главная страница

---

Эта документация поможет вам быстро объяснить проект и понять его структуру! 🚀
